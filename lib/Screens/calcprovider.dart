import 'package:flutter/material.dart';

class Calcprovider extends StatelessWidget {
  const Calcprovider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('hello'),
    );
  }
}